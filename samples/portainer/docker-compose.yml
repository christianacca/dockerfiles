version: '3.7'

services:

  portainer:
    image: portainer/portainer:latest
    isolation: process
    ports:
      - 9000:9000
    volumes:
      - "portainer_data:c:/data"
    networks:
      - portainer_net

volumes:
  portainer_data:

networks:
  portainer_net: